services:
  - type: web
    name: vs-discord-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: OCR_SPACE_API_KEY
        sync: false
    plan: free
